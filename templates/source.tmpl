var Marty = require('marty');
var {{modelName}}ActionCreators = require('actions/{{modelName}}ActionCreators');

var {{modelName}}HttpAPI = Marty.createStateSource({
  rootUrl: {{sourceEndpoint}},

  create: function({{paramModelName}}) {
    this.post({
      url: this.rootUrl,
      body: {{paramModelName}}
    }).then(function(response) {
      {{modelName}}ActionCreators.create(response.body);
    }, function(errorResponse) {
      if(errorResponse.hasBody) {
        errorResponse.json().then(function() {}, function() {});
      }
    });
  },

  getAll: function() {
    this.get(
      this.rootUrl
    ).then(function(response) {
      {{modelName}}ActionCreators.readAll(response.body);
    }, function(errorResponse) {
      if(errorResponse.hasBody) {
        errorResponse.json().then(function() {}, function() {});
      }
    });
  },

  getById: function(id) {
    var url = this.rootUrl + id;
    this.get(
      url
    ).then(function(response) {
      {{modelName}}ActionCreators.receive([response.body]);
    }, function(errorResponse) {
      if(errorResponse.hasBody) {
        errorResponse.json().then(function() {}, function() {});
      }
    });
  },

  update: function(id, {{paramModelName}}) {
    var url = this.rootUrl + id;
    this.put({
      url: url,
      body: {{paramModelName}}
    }).then(function(response) {
      {{modelName}}ActionCreators.update(response.body);
    }, function(errorResponse) {
      if(errorResponse.hasBody) {
        errorResponse.json().then(function() {}, function() {});
      }
    });

  },

  destroy: function(id) {
    var url = this.rootUrl + id
    this.delete(
      url
    ).then(function(response) {
      {{modelName}}AcionCreators.destroy(id);
    }, function(errorResponse) {
      if(errorResponse.hasBody) {
        errorResponse.json().then(function() {}, function() {});
      }
    });
  },
});

module.exports = {{modelName}}HttpAPI;
